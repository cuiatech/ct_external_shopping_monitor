name: ct_external_shopping_monitor
description: 
version: 0.0.1
homepage:

environment:
  sdk: '>=2.19.1 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dependency_overrides:
  ct_micro_commons_dependencies:
    path: ../ct_micro_commons_dependencies
    # git:
    #   url: "https://github.com/cuiatech/ct_micro_commons_dependencies.git"
    #   ref: "master"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  
flutter:


